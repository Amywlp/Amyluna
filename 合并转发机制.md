# 合并转发（send_group_forward_msg）机制详解

## 概述

合并转发是 QQ 群聊中的一种消息发送方式，将多条消息打包成一个合并转发卡片展示。在 OneBot v11 协议中对应 `send_group_forward_msg` action，由 SnowLuma 适配层实现。

amyluna v2 在后处理阶段，当回复的纯文本长度超过 50 字符时，自动使用合并转发方式发送（而非普通群消息）。

---

## 1. 协议规范

### 1.1 OneBot v11 Action

```
action: send_group_forward_msg
```

**请求参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `group_id` | `number` | 目标群号 |
| `messages` | `ForwardNode[]` | 合并转发消息节点数组 |

**返回：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `message_id` | `number` | 发送成功后的消息 ID |
| `res_id` | `string` (可选) | 转发资源 ID |
| `forward_id` | `string` (可选) | 转发 ID |

### 1.2 ForwardNode 结构

每个节点代表合并转发卡片中的一条消息：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `uin` | `number` | 否 | 发送者 QQ 号（与 `user_id` 等效） |
| `user_id` | `number` | 否 | 发送者 QQ 号（与 `uin` 等效） |
| `name` | `string` | 否 | 发送者显示名称（优先级高于 `nickname`） |
| `nickname` | `string` | 否 | 发送者显示名称 |
| `content` | `string \| MessageSegment[] \| ForwardNode[]` | 否 | 消息内容（文本、段数组或嵌套节点） |
| `message` | `string \| MessageSegment[] \| ForwardNode[]` | 否 | 消息内容（与 `content` 等效） |
| `time` | `number` | 否 | 消息时间戳（Unix 秒），不设则使用当前时间 |

---

## 2. amyluna 中的实现

### 2.1 触发条件

**文件：** `src/convmgr/post-process.ts`

```typescript
const FORWARD_TEXT_THRESHOLD = 50;

// 纯文本长度（去除 URL、CQ 码、图片占位、空白后）> 50 → 使用合并转发
const sendMethod = plainLen > FORWARD_TEXT_THRESHOLD ? "forward" : "normal";
```

决策逻辑：

1. 仅对白名单群 (`WHITELIST_GROUP_IDS`) 生效
2. 只有回复内容纯文本长度超过 50 字符才触发
3. 非白名单群始终使用 `normal` 发送方式

### 2.2 调用链路

```
P3 (LLM 生成回复)
  → P2 convmgr (后处理 + 构造 forward_nodes)
    → IPC send_message → P1 receiver
      → sendGroupForwardMessage() → WS → SnowLuma → QQ
```

### 2.3 节点构造（当前实现）

**文件：** `src/convmgr/ipc-server.ts` (三处：chat_response、intermediate_reply、handleTrigger)

```typescript
forward_nodes: [{ uin: deps.botId, content: trimmed }]
```

当前实现只传 `uin`（bot 自己的 QQ 号）和 `content`，**未设置 `name`/`nickname`**。

### 2.4 发送实现

**文件：** `src/receiver/send-api.ts`

```typescript
export async function sendGroupForwardMessage(
  client: WsClient,
  groupId: number,
  nodes: ForwardNode[],
): Promise<SendForwardResult> {
  return client.request<SendForwardResult>("send_group_forward_msg", {
    group_id: groupId,
    messages: nodes,
  });
}
```

### 2.5 IPC 类型定义

**文件：** `src/common/types/ipc.ts`

```typescript
export interface ForwardNode {
  name?: string;
  uin?: number;
  content?: string;
}

export interface SendMessagePayload {
  group_id: number;
  message: string;
  method?: "normal" | "forward";
  meme_url?: string;
  forward_nodes?: ForwardNode[];
}
```

### 2.6 接收解析

**文件：** `src/receiver/message-saver.ts`

收到合并转发消息时，调用 `get_forward_msg` 获取完整内容，解析为：

```
[合并转发消息]
发送者名称(user_id): 消息文本
发送者名称(user_id): 消息文本
...
```

存储到数据库的 `merged_forward` 列。

---

## 3. 发送者身份伪造分析

### 3.1 测试结论总表

通过向群 **<GROUP_ID>** 发送多轮测试消息，得出以下完整结论：

| uid 类型 | 是否设置 name | 显示效果 |
|----------|:-----------:|----------|
| 群内成员（bot/群友） | 有 | ❌ **name 无效**，强制显示该群友真实群昵称 + 真实头像 |
| 群内成员 | 无 | 真实群昵称 + 真实头像 |
| 非群内真实 QQ 用户 | 有 | ✅ **name 生效**，该用户的真实头像/挂件/气泡 + 伪造的名称 |
| 非群内真实 QQ 用户 | 无 | 显示 uid 数字 + 真实头像 |
| 不存在的随机 uid | 有 | ✅ **name 生效**，随机默认头像 + 伪造的名称 |
| 不存在的随机 uid | 无 | 显示 uid 数字 + 随机头像 |
| 无 uid / uid=0 | 有/无 | 均视为 bot 发送，显示 bot 真实昵称 |

### 3.2 核心规则

1. **群内用户强实名**：QQ 客户端对群内成员 uid 强制拉取群名片（card）或 QQ 昵称覆盖，`name`/`nickname` 字段完全无效
2. **非群内 uid 无限制**：`name` 字段完全可控，可设为任意文本（中文、英文、emoji）
3. **头像/装扮跟随 QQ 号**：uid 指向真实用户时，客户端自动拉取该用户的头像、头像挂件、消息泡装扮
4. **不存在 uid 用随机头像**：uid 无对应用户时显示系统默认随机头像
5. **`name` 和 `nickname` 行为一致**，同时存在时 `name` 优先
6. **发送目标群无限制**：合并转发可发往 bot 所在的任意群，即便构造的 uid 不在目标群内也能成功

### 3.3 可伪造项

| 要素 | 条件 | 风险等级 |
|------|------|:--------:|
| 昵称 | uid 不在目标群 | 🔴 高 — 任意文本 |
| 头像 | uid 为真实 QQ 用户 | 🟡 中 — 自动拉取该用户真实头像 |
| 头像挂件 | uid 为真实 QQ 用户 | 🟡 中 — 自动拉取 |
| 消息泡装扮 | uid 为真实 QQ 用户 | 🟡 中 — 自动拉取 |
| 消息时间 | 任意 | 🟢 低 — `time` 字段可控 |

### 3.4 不可伪造项

| 要素 | 原因 |
|------|------|
| 群内成员身份发言 | uid 在群内时，客户端强制使用真实群昵称和头像 |
| QQ 号本身 | 需真实存在的 QQ 号来借头像/装扮；不存在则用随机头像 |

### 3.5 最高风险场景

**找一个不在目标群里的真实 QQ 用户 uid**，可做到：

- 显示该用户的真实头像、头像挂件、消息泡装扮
- 自定义任意昵称
- 群友看到头像和装扮与该用户平时一致，不会起疑
- 实现完美的身份冒充

---

## 4. 测试脚本

测试脚本位于 `/home/USER/temp_files/`，均使用原生 WebSocket 直连 SnowLuma：

| 脚本 | 测试内容 |
|------|----------|
| `test-forward-name.mjs` | 基础测试：bot uin + 不同 name/nickname 组合 |
| `test-forward-name2.mjs` | 无 uin / uin=0 边界测试 |
| `test-forward-name3.mjs` | 使用群友 uin（<MURI_BOT_ID>） |
| `test-forward-name4.mjs` | 非群内用户 uid（<OTHER_UID>）+ 伪造昵称 |
| `test-forward-name5.mjs` | 随机 11 位不存在的 uid + 伪造昵称 |
| `test-forward-name6.mjs` | 向 bot 不在的群发送 |
| `test-forward-name7.mjs` | 指定群 + 指定 uid + 自定义 name |

### 4.1 通用脚本模板

```javascript
import pkg from "<amyluna>/node_modules/ws/index.js";
const { WebSocket } = pkg;

const WS_URL = "ws://127.0.0.1:3001?access_token=<token>";

const ws = new WebSocket(WS_URL);

ws.on("open", () => {
  ws.send(JSON.stringify({
    action: "send_group_forward_msg",
    params: {
      group_id: <GROUP_ID>,
      messages: [
        { uin: <BOT_ID>, content: "消息1" },
        { uin: <OTHER_UID>, name: "自定义名", content: "消息2" },
      ],
    },
    echo: `test-${Date.now()}`,
  }));
});

ws.on("message", (raw) => {
  const msg = JSON.parse(raw.toString());
  console.log("响应:", msg.status, msg.retcode, msg.data);
  ws.close();
});
```

---
#此板块忽略
## 5. 对 amyluna 的安全建议

### 5.1 当前状态

amyluna 合并转发只用 bot 自己的 uin（`<BOT_ID>`），bot 在群内，`name` 无法生效，**当前实现是安全的**。

### 5.2 如果要支持自定义角色名

若未来想让合并转发卡片显示角色名（如 "赫萝"），需要：

1. 使用一个**不在目标群的** uin（可以是 bot 的小号或专门注册的号）
2. 设置 `name` 为角色名
3. 注意：该 uin 的真实头像会被拉取，如果角色需要特定头像，需要使用无真实用户的随机 uid（此时头像为系统默认随机头像）

### 5.3 防御建议

- **不要信任合并转发中的发送者身份**：uid 和 name 均可被伪造，合并转发卡片中的 "某人说了什么" 不可作为身份证据
- **关注消息本身的可信度**：合并转发的内容也可以任意编造（节点 content 可与原始消息完全不同）
- `get_forward_msg` 获取已发送合并转发时，返回的 `sender` 信息是真实的（来自 QQ 服务端），但 `send_group_forward_msg` 发送时构造的 name/uin 不受校验

---

## 6. 相关文件索引

| 文件 | 内容 |
|------|------|
| `src/receiver/send-api.ts:18-26` | `ForwardNode` 类型（完整版，含所有字段） |
| `src/receiver/send-api.ts:56-72` | `sendGroupForwardMessage()` 发送函数 |
| `src/receiver/send-api.ts:46-54` | `getForwardMsg()` 接收解析函数 |
| `src/receiver/ipc-server.ts:66-68` | P1 层分派 forward 发送 |
| `src/receiver/message-saver.ts:71-89` | 接收合并转发消息解析 |
| `src/common/types/ipc.ts:29-33` | IPC 层 `ForwardNode` 类型 |
| `src/common/types/ipc.ts:36-44` | IPC 层 `SendMessagePayload` 类型 |
| `src/convmgr/ipc-server.ts:93-95` | P2 chat_response 构造 forward_nodes |
| `src/convmgr/ipc-server.ts:149-151` | P2 intermediate_reply 构造 forward_nodes |
| `src/convmgr/ipc-server.ts:246-248` | P2 handleTrigger 构造 forward_nodes |
| `src/convmgr/post-process.ts:41` | `FORWARD_TEXT_THRESHOLD = 50` 触发阈值 |
| `src/common/db/pool.ts:57` | DB `merged_forward` 列 |
| `snowluma_adapter/src/snowluma/api.ts:17-33` | v1 版 `ForwardNode` 类型 |
| `snowluma_adapter/src/snowluma/api.ts:66-82` | v1 版 `sendGroupForwardMessage()` |
| `snowluma_adapter/src/chat/manager.ts:410-414` | v1 版构造 forward_nodes（含 `nickname: "Bot"`） |
